import { Content } from ".";
import { ContentDemoFull } from "../../demo_data";

const meta = {
	title: "Prometheus/Functional/Content",
	component: Content,
};
export default meta;

export const Primary = () => (
	<ContentDemoFull />
);
