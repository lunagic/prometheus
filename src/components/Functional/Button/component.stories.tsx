import { Button } from ".";

const meta = {
	title: "Prometheus/Functional/Button",
	component: Button,
};
export default meta;

export const Primary = () => <Button onClick={() => { }}>Hello</Button>;
